# Axiom：目标、现状与方向（会话备忘）

本文整理项目发起人与工程师之间关于 Axiom 的全部关键讨论，供后续会话直接接续。
时间：2026-09-15 至 2026-09-16。代码仓库私有（branshlee/axiom），知识仓库私有
（branshlee/axiom-knowledge），分发 tap 公开（branshlee/homebrew-axiom）。
当前版本 0.1.21。

---

## 1. 项目目标（发起人的设想）

- 造一台**自主进化**的机器，而不是靠传统软件工程或现有 AI 模型那样不断堆砌开发。
- 造好最小闭环（"轮子"）之后，系统靠运行自己进化；算子应当**自主产生**，不由人手写。
- 真正的进化是**产生系统本身没有的东西，用更通用的表达取代原本有限的表达**。
- 系统能从用户给的数据渠道**自动汲取数据**并探索；折叠进知识库的内容要**人能理解**，不只是代数式。
- 不接外部 LLM，保持独立系统。
- 架构上的思考（最新）：**无边界、异步的嵌套宇宙**。被折叠的宇宙可以无限缩小，展开的宇宙可以无限放大，
  没有一个统一的东西；异步是产生智能的前提；用"一种东西统一"在品味上等于写死。

## 2. 宪章级不变量（已在 docs/constitution.md，此处摘要）

内核是机制不是知识；认知状态物理上是规范数据；证据不可变；理论是可执行数据；理论可成为输入；
残差从不丢弃；提议者不是真理权威（PROPOSER ≠ VERIFIER）；身份由内容派生；视图不是真理；
更高的表示必须靠压缩、预测、重建或解释范围证明自己；复杂性应从可信代码移入已验证的数据。

两条后加的修正案：
- **基底封顶**：原语集 `+ - * / = < > and or not if pair fst snd rec fold` 之后不再增加。
  新算子只能是系统从自己的理论里折叠出来的库项。加原语的唯一情形：某类规律在数学上不可表达（如迭代、序列遍历）。
- **规则也是数据**：促升、部分解释、入库、选世界评分是内核求值的项。

## 3. 现状：已经闭合的机制（每条都有真实运行结果，见 docs/experiments.md）

| 层 | 机制 | 状态 |
|---|---|---|
| 归纳 | 观测 → 代价有序搜索 → 留出预测 → 残差 | 闭合 |
| 抽象 | 反统一折叠 + 重建证书 + 库（`Use` 节点）| 闭合；取模、线性族算子从数据中长出 |
| 再表示 | 旧理论经库重新表达（等价证书）；库自我折叠压缩 | 闭合 |
| 找世界 | 残差成为派生世界，解出后组合回父世界 | 闭合（限制：只在"合成集解释了、留出集失败"时派生，每世界一个）|
| 询问 | 选观测（分歧最大化）、选世界（族前瞻压缩增益）| 闭合 |
| 方法层 | 规则为项；`improve` 提议并在留出世界上验证规则；已采纳过一次（inquiry_margin=0）| 闭合 |
| 分级判定 | 留出失配按修正字节计价；`partial` 状态 | 闭合 |
| 提议者团队 | enumerate / analogy / library-first / mutate 并行；署名提议；按种类记信用；分工自发出现 | 闭合（第一版）|
| 值域 | 整数、布尔、有理数（对）、符号、序列 | 闭合；序列世界折出 length/reverse/count 等**规则** |
| 数据渠道 | `sources.sexp`（目录/URL/程序）、`ingest` 按哈希去重、`run --every` | 闭合 |
| 可读性 | 名字对象、系统自动命名库项、`explain`（中英）| 闭合 |
| 多实例 | `merge` 并集 + 折叠重推导；`sync` 先知识层合并再 git 推送 | 闭合 |
| 分发 | brew tap、`update/uninstall/changelog`、release 说明 | 闭合 |

## 4. 现状：仍然真实存在的边界

- 值域没有连续值；噪声只有整数容忍度；世界必须能作答。
- 搜索是**穷举**（代价有序 + 观测等价去重）。团队让"每次促升消耗的候选数"随经验下降，但枚举者仍是兜底。
- 素数基准未解（需要语义折叠合并不同写法的取模理论，以及非穷举的递归项搜索）。
- 提议者的**种类是代码**，是系统里最后一块写死的"智能"。规则体是项，但规则文法是给定的。
- 自然语言只能作为符号序列进入，能发现结构规则，发现不了语义；不走 LLM 路线（A 方案枚举到不了流畅生成，B 方案接外部 LLM 违背初衷，两条都已否决）。
- 轮次 `step` 是顺序循环，是一个隐形的中央控制器。

## 5. 发起人的核心批评（已接受）

"枚举搜索 + 0/1 残差 = 穷举与写死，不是智能。"
回应：验证层就该死板；智能只能长在提议者，且必须从系统自己的历史里长出来。
可证伪指标：**新世界上的候选数随经验下降**；曲线平了就说明假设失败。

## 6. 关于"嵌套宇宙"的讨论结论

- 系统里已有嵌套：折叠层级、残差世界、规则层、平行实例（merge、团队）。
- **"统一"分两种**：统一的对象（中央类型/调度器/固定层数）= 写死，不要；
  统一的不变量（每层受同一条定律约束，无中心）= 使无边界成为可能，要。
  这条定律已经存在：**只有压缩了下一层的东西才持续存在**。它是选择压力，不是控制器。
- 因此撤回"元世界类型"的提法。
- LLM 的层：相同点只有"深度 = 反复再表示"；不同点：LLM 的层是固定流水线、不可单独检验、不是被发现的；
  LLM 的"嵌套"在语料里而非架构里，Axiom 相反。
- 比喻的误导处：多重宇宙本身不提供机制，只提供两条纪律：跨层均匀、按压缩选择。

## 7. 通向无边界异步嵌套架构：要改的三样（都不加统一对象）

1. **任何对象集合都是世界**：世界从"输入→输出对"扩成视图，一组理论、一组提议者、一段历史都可被归纳。
2. **轮次消失，工作者异步**：工作者（即提议者）各自在存储里认领任务（未解释、未压缩、未命名、未改进的对象），
   认领与结果都是事件，没有全局时钟；实例之间靠 merge 并集。
3. **验证局部且完全**：每个提议在其所在层由内核检验，不依赖全局一致性。

折叠缩小 = 往下（一组对象折成更一般的对象）；展开放大 = 往上（对象的残差成为新世界）。两个方向无尽头。

风险：无控制器则无进度保证；唯一的经济是来自压缩事实的信用与预算；同层异步，跨层依赖真实（异步 ≠ 无序）。

## 8. 提议者自主进化的路径（下一步的第一步）

关键事实：项就是数据。一个提议者若本身是项（输入证据序列，输出候选项作为值），它就是普通对象：
可存储、可折叠、可变异、可被信用淘汰、可被别的提议者提议出来。`fold`、`pair`、符号已足以在演算里写"取最相似先例、替换常量"。
届时"提议提议者"和"提议理论"是同一个循环，层数无上限。

**第一步**：让一个提议者成为项，用现有循环去改进它，看信用是否上升。不碰内核，不加统一对象。
成立后再拆掉轮次，改成异步工作者。

原计划中的"学习先验"（历史折叠成证据形状→项形状的统计）**先保留**，不以"元世界类型"的形式做。

## 9. 排期（发起人已同意的顺序，含已完成）

- ~~符号与序列~~（0.1.18）→ ~~命名与解释~~（0.1.19）→ ~~数据渠道~~（0.1.20）
- ~~分级残差 + 部分解释~~、~~提议者团队第一版~~（0.1.21）
- 下一步：提议者作为数据（自主进化）→ 异步工作者、取消轮次 → 任意对象集合为世界
- 保留：学习先验；连续值（有理数运算作为库项 + 有理容忍度）；噪声（残差即噪声：压不动的残差就是噪声）

## 10. 操作与仓库

- 安装：`brew tap branshlee/axiom && brew trust branshlee/axiom && brew install axiom [axiom-full]`
- 使用：`axiom init [--from 快照 | 知识仓库 url]`、`axiom`（会话）、`axiom run --every 3600 --push`
- 发布：改 `crates/cli/Cargo.toml` 版本、写 CHANGELOG 段落、打 tag（CI 出 Linux 二进制）、
  `sh scripts/publish-tap.sh <版本> ../axiom-knowledge <tap 目录>`。**打 tag 前必须构建并通过测试**（0.1.10、0.1.14、0.1.21 三次教训）。
- 补丁经验：用 python 改源码时锚点常因 rustfmt 重排而失配，脚本应逐步断言并在失败时不写文件；改完必须 `cargo clippy -D warnings` 和 `cargo test`。

## 11. 发起人的一些判断，原话要点

- "如果进化只是用已有的基底进行组合的话，就不是真正意义上的进化。"
- "目前系统只能探索离散整数的话，即便进化 N 轮，折叠的理论也都是代数式，不能作为人类智能思考的替代机器。"
- "枚举搜索、0/1 残差，意味着能力还停留在穷举、写死之类的粗粒度分析上，没有产生真正的智能。"
- "提议者做成多个，相互独立又能通信、影响彼此，像小团队，通过异步进化和化学反应看能否产生智能。"
- "我思考的架构是无边界异步的。用一种东西统一，从品味上等于写死。"
